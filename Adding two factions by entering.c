#include <stdio.h>
#include <conio.h>

int main()
{
    float x,y,sum;
    printf("Enter the first number = ");
    scanf("%f", &x);
    printf("Enter the second number = ");
    scanf("%f", &y);
    sum=x+y;
    printf("The result is = %f",sum);
    return 0;
}
